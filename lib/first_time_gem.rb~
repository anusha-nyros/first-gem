class First_gem
  def self.hi
    puts "Hello world!"
    time = Time.now
    minute = time.min
    hour = time.hour % 12
    meridian_indicator = time.hour < 12 ? 'AM' : 'PM'
    puts "#{minute} minutes past #{hour} o'clock, #{meridian_indicator}"
    
  end
end
